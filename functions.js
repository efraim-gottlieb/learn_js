// function myFirstFunc(name, age) {
//   return `${name.toUpperCase()}, your born year is ${new Date().getFullYear() - age}`
// }

// console.log(myFirstFunc("efraim", 21));

// calculator
// const add = (x, y) => x + y;
// const subtract = (x, y) => x - y;
// const multiply = (x, y) => x * y;
// const divide = (x, y) => x / y;

// const calculator = {
//   add: (x, y) => x + y,
//   subtract: (x, y) => x - y,
//   multiply: (x, y) => x * y,
//   divide: (x, y) => x / y,
// };

// console.log(calculator.add(1, 2));
// console.log(calculator.subtract(1, 2));
// console.log(calculator.multiply(1, 2));
// console.log(calculator.divide(1, 2));


